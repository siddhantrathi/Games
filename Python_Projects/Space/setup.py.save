from setuptools import setup

APP = ['Space_Invader.py']
DATA_FILES = ['background.png','background.wav  enemy.png       PixelFJVerdana12pt.ttf']
OPTIONS = {
 'iconfile':'logoapp.icns',
 'argv_emulation': True,
 'packages': ['certifi'],
}

setup(
    app=APP,
    data_files=DATA_FILES,
    options={'py2app': OPTIONS},
    setup_requires=['py2app'],
)
